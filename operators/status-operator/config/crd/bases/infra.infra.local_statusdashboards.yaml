---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: statusdashboards.infra.infra.local
spec:
  group: infra.infra.local
  names:
    kind: StatusDashboard
    listKind: StatusDashboardList
    plural: statusdashboards
    singular: statusdashboard
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StatusDashboard is the Schema for the statusdashboards API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StatusDashboardSpec defines the desired state of StatusDashboard
            properties:
              refreshIntervalSeconds:
                description: RefreshIntervalSeconds defines how often the operator
                  should refresh the status
                type: integer
            type: object
          status:
            description: StatusDashboardStatus defines the observed state of StatusDashboard
            properties:
              componentStatuses:
                description: ComponentStatuses lists the status of each monitored
                  component
                items:
                  description: ComponentStatus defines the status of an individual
                    infrastructure component
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    status:
                      type: string
                  required:
                  - name
                  - status
                  type: object
                type: array
              lastUpdated:
                description: LastUpdated is the last time the dashboard was updated
                format: date-time
                type: string
              overallHealth:
                description: OverallHealth can be Healthy, Warning, or Critical
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
